#
# Pushup Counter (local dev)
#
# Use the Apps Script /exec URL directly for local development.
PUBLIC_PUSHUP_WEBHOOK_URL=
PUBLIC_PUSHUP_PASSCODE=1021
#
# Pushup Counter (production via Cloudflare Pages Functions)
#
# Configure these as Cloudflare Pages environment variables (server-side).
PUSHUP_WEBHOOK_URL=
PUSHUP_PASSCODE=1021
PUSHUP_COOKIE_SECRET=

